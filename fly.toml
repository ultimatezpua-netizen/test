app = "flyctl-fix"
primary_region = "waw"

[build]
  dockerfile = "Dockerfile"

# CLI tool configuration - no web service needed
[env]
  GO_ENV = "production"

# No http_service section needed for CLI tool
# The application runs as a command-line tool, not a web service

[[vm]]
  size = "shared-cpu-1x"